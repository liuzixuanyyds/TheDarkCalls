#include <bits/stdc++.h>
#include <windows.h>
#define Inf 0xffffff
#define up(i,d,u)   for(int i = (d);i <= (u);i++)
#define down(i,u,d) for(int i = (u);i >= (d);i--)
using namespace std;

class maze
{
    public:
        maze(void);
        ~maze(void);
};